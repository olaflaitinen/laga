# Contributing

Follow the instructions in [CONTRIBUTING.md](../CONTRIBUTING.md) for setup, testing, and pull request checks.

## Before You Send A Change

1. Run `make lint`.
2. Run `make typecheck`.
3. Run `make test`.
4. Rebuild the package with `make build` if you touched packaging or metadata.
